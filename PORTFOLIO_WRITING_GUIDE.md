# Portfolio Writing Guide

이 문서는 포트폴리오 프로젝트 설명을 작성할 때 사용할 내용 구성 기준이다.  
목표는 "무엇을 만들었는가"보다 "어떤 문제를 어떤 판단으로 해결했는가"가 먼저 읽히도록 만드는 것이다.

## Core Principle

포트폴리오의 프로젝트 설명은 기능 목록이 아니라 문제 해결 기록이어야 한다.

좋은 설명은 아래 흐름을 가진다.

```text
문제 상황
-> 기술적 판단
-> 적용한 구조/구현
-> 운영 효과 또는 검증 가능한 결과
```

나쁜 설명은 구현한 기능만 나열한다.

```text
대시보드 구현
데이터 조회 기능 구현
알림 기능 구현
배포 자동화 구축
```

좋은 설명은 문제와 결과를 함께 말한다.

```text
수집 중단을 뒤늦게 발견하던 운영 문제를 해결하기 위해
수집 상태, 장애 여부, 데이터 품질을 한 화면에서 확인하는 관제 구조를 설계했다.
```

## What To Emphasize

프로젝트마다 다음 중 실제로 강한 요소를 우선 강조한다.

1. 운영 규모

- 실제 운영 기간
- 사용자 수, DAU, 월간 사용 인원
- 실제 수업, 연구 과제, 회사 서비스 등 사용 맥락

2. 문제의 구체성

- 기존에는 어떤 불편이 있었는지
- 문제가 얼마나 늦게 발견됐는지
- 어떤 운영 리스크가 있었는지
- 누가 불편을 겪었는지

3. 기술적 판단

- 왜 해당 기술을 선택했는지
- 어떤 제약 조건이 있었는지
- 어떤 대안보다 이 방식이 적절했는지
- 성능, 운영, 보안, 비용 중 무엇을 우선했는지

4. 개선 결과

- 수치 개선이 있으면 가장 강하게 사용한다.
- 수치가 없으면 운영 방식의 변화로 표현한다.
- 예: 수동 점검 감소, 장애 인지 시간 단축, 배포 재현성 확보, 운영 가시성 확보

5. 증거

- GitHub
- Docs
- 실제 화면
- 아키텍처 이미지
- 운영 기간, 사용자 수, 로그/리포트 흐름

## Title Rules

성과 제목은 기능명이 아니라 문제 해결 단위로 작성한다.

### Avoid

```text
통합 관제 대시보드 구현
데이터 조회 기능 구현
Slack 알림 기능 구현
Docker Compose 배포
```

### Prefer

```text
수집 중단을 뒤늦게 발견하던 운영 문제 해결
분산된 수집 상태를 한 화면에서 추적하는 관제 구조 설계
수집 지연/중단 자동 감지 및 실시간 알림 체계 구축
반복 점검 업무를 줄이기 위한 리포트 자동화
제한된 내부망 환경에서 self-hosted runner 기반 배포 자동화
```

제목은 가능하면 아래 중 하나의 형태를 따른다.

```text
[문제] 해결
[운영 문제] 개선
[제약 조건]을 고려한 [구조] 구축
[반복 작업] 자동화
[성능/보안/운영] 개선을 위한 [기술적 선택]
```

## Section Structure

프로젝트 상세 페이지는 다음 순서를 기본으로 한다.

```text
개요
역할
기술 스택
주요 성과
회고 또는 배운 점
```

각 섹션의 목적은 다르다.

### 개요

프로젝트의 존재 이유와 실제 운영 맥락을 설명한다.

포함할 것:

- 어떤 문제를 다루는 프로젝트인지
- 누가 사용하는지
- 실제 운영 여부
- 운영 기간 또는 사용자 규모
- 가장 중요한 결과 한 가지

문장 예시:

```text
이 프로젝트는 연구실 AI 데이터 수집 환경에서 수집 상태, 장애 여부, 데이터 품질을 통합 모니터링하기 위해 만든 관제 시스템입니다.
개발 이후 약 2년간 연구 과제 환경에서 실제 운영되고 있으며, 수집 중단과 이상 데이터를 더 빠르게 인지할 수 있도록 설계했습니다.
```

### 역할

담당 범위를 직무명보다 책임 단위로 쓴다.

Avoid:

```text
Backend Developer
Frontend Developer
DevOps Engineer
```

Prefer:

```text
요구사항 정리, 기능 범위 정의, 전체 아키텍처 설계 주도
데이터 수집, 조회, 장애 감지 흐름을 고려한 DB 스키마 설계 및 API 개발
운영자가 상태를 빠르게 파악할 수 있는 관제 UI 설계 및 구현
Docker 기반 배포 환경 구성과 CI/CD 파이프라인 구축
```

### 기술 스택

기술 스택은 "사용한 기술 목록"으로만 보이지 않게 한다.

좋은 기술 스택 설명은 다음을 보조한다.

- 핵심 기술 2~3개를 먼저 강조
- 나머지는 실제 사용 기술로 균등하게 표시
- 기술별 분야 구분은 기술 수가 충분할 때만 사용
- 기술을 과장하지 않는다.

### 주요 성과

주요 성과는 3~5개로 제한한다.  
각 항목은 아래 구조를 따른다.

```text
제목: 문제 해결 단위
본문 1: 기존 문제 또는 제약
본문 2: 내가 설계/구현한 방식
본문 3: 달라진 결과
```

예시:

```text
01. 수집 중단을 뒤늦게 발견하던 운영 문제 해결

기존 파이프라인은 수집이 중단되거나 데이터에 문제가 발생해도 직접 데이터를 열람하기 전까지 이상 여부를 확인하기 어려웠습니다.
이를 해결하기 위해 수집 상태, 장애 여부, 데이터 품질을 한 화면에서 확인하는 관제 구조를 설계했습니다.
운영자가 수집 지연과 중단 상황을 빠르게 인지할 수 있는 환경을 구축했습니다.
```

## AS-IS / TO-BE Pattern

문제가 복잡하거나 기술적 판단이 중요한 항목은 AS-IS / TO-BE 구조를 사용한다.

```text
AS-IS
기존 구조에서는 어떤 문제가 있었는지 쓴다.

TO-BE
문제를 해결하기 위해 어떤 구조나 기술을 적용했는지 쓴다.

Result
그 결과 무엇이 개선됐는지 쓴다.
```

이 구조는 특히 다음 주제에 적합하다.

- 성능 최적화
- 장애 감지
- 배포 자동화
- 인증/인가
- 동시성 제어
- 인프라 개선
- 데이터 파이프라인 개선

## Sentence Rules

문장은 주장보다 근거가 먼저 보이도록 쓴다.

### Avoid

```text
운영 효율을 개선했습니다.
안정성을 확보했습니다.
사용자 편의성을 개선했습니다.
```

### Prefer

```text
관리자가 직접 데이터를 열람해야만 이상 여부를 확인할 수 있던 흐름을 개선해, 수집 상태와 장애 여부를 대시보드에서 확인할 수 있도록 설계했습니다.
```

### Strong Verbs

다음 동사를 우선 사용한다.

- 설계했다
- 분리했다
- 자동화했다
- 추적했다
- 감지했다
- 검증했다
- 개선했다
- 전환했다
- 통합했다
- 재현 가능하게 만들었다

다음 표현은 너무 자주 쓰지 않는다.

- 구현했다
- 개발했다
- 제공했다
- 구축했다

## Evidence Rules

수치가 있으면 반드시 제목 또는 첫 문단 근처에 둔다.

좋은 수치:

- 운영 기간
- 사용자 수
- DAU / MAU
- 처리 시간 전후
- 비용 절감률
- 장애 인지 시간
- 배포 시간
- 자동화 전후 작업량

수치가 없으면 운영 변화로 쓴다.

```text
수동 점검에 의존하던 흐름에서 벗어나, 수집 지연과 중단을 시스템이 자동으로 감지하도록 개선했습니다.
```

## Project-Specific Direction

### Panopticon

가장 강한 스토리:

```text
데이터 수집이 멈췄는데도 3일간 인지하지 못했던 운영 문제를 해결하기 위해 만든 관제 시스템
```

강조할 것:

- 수집 중단을 늦게 발견하던 문제
- 데이터 품질을 직접 열람해야만 확인할 수 있던 문제
- 수집 상태, 장애 여부, 데이터 품질의 통합 모니터링
- Slack 알림과 리포트 자동화
- self-hosted runner 기반 내부망 배포 자동화

추천 주요 성과 제목:

```text
01. 수집 중단을 뒤늦게 발견하던 운영 문제 해결
02. 수집 상태와 데이터 품질을 한 화면에서 추적하는 관제 구조 설계
03. 수집 지연/중단 자동 감지 및 실시간 알림 체계 구축
04. 반복 점검 업무를 줄이기 위한 리포트 자동화
05. 제한된 내부망 환경에서 self-hosted runner 기반 배포 자동화
```

### SCH MiniProject

가장 강한 스토리:

```text
학과 ML/DL 강의에서 실제로 10개월간 사용된 과제 제출 및 랭킹 관리 시스템
```

강조할 것:

- 실제 강의 운영에 사용
- 월 평균 약 40명 사용
- 과제 제출, 점수 관리, 랭킹 산정의 자동화
- 성적 평가 시스템에 맞춘 인증/인가와 보안
- 감사 로그와 파일 업로드 검증

추천 주요 성과 제목:

```text
01. 실제 강의 운영에 사용된 과제 제출 흐름 자동화
02. 성적 평가 환경을 고려한 세션 기반 인증/인가 구조 설계
03. 운영 이력 추적을 위한 감사 로그 시스템 구축
04. 과제 파일 업로드 과정의 보안 검증 강화
05. Docker Compose 기반 실행 환경 통일과 배포 재현성 확보
```

### DobongLife Infrastructure

가장 강한 스토리:

```text
초기 단일 서버 배포에서 역할 기반 AWS 인프라와 Terraform 기반 운영 구조로 고도화한 프로젝트
```

강조할 것:

- V1에서 빠른 배포와 최소 비용을 우선한 판단
- V2에서 FE/BE/nginx 역할 분리
- SSH 접근 제거, SSM과 OIDC 기반 배포 전환
- Terraform으로 인프라 재현성 확보
- CloudWatch 기반 로그 수집

추천 주요 성과 제목:

```text
01. 초기 서비스 배포를 위한 단일 EC2 기반 AWS 인프라 구축
02. 역할 기반 EC2 분리로 외부 노출 영역과 내부 서비스 영역 분리
03. SSH 접근 제거와 SSM/OIDC 기반 배포 구조 전환
04. Terraform 기반 인프라 코드화로 재현성 확보
05. CloudWatch 로그 수집을 통한 운영 가시성 확보
```

## Review Checklist

작성 후 아래 질문으로 검토한다.

- 제목만 읽어도 해결한 문제가 보이는가?
- "구현했다"보다 "왜 필요했는지"가 먼저 보이는가?
- 실제 운영 맥락이 들어가 있는가?
- 기술 선택의 이유가 최소 한 번 이상 드러나는가?
- 성과가 기능 목록으로만 보이지 않는가?
- 수치 또는 운영 변화가 포함되어 있는가?
- 과장된 표현 없이 검증 가능한 말로 쓰였는가?
- 한 항목에 너무 많은 내용을 넣지 않았는가?

